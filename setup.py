import sys
from distutils.core import setup, Extension
from setuptools import setup, find_packages


setup(
    name="fortunetelling",
    version="0.0.1",
    packages=['fortunetelling'],
    py_modules="fortunetelling",
    author="",
    description="",
    url="",
    entry_points={
    },
    install_requires=[
    ]
)
